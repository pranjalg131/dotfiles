unbind-key C-b
bind-key ` send-prefix
set -g prefix `
set -g base-index 1
set -g pane-base-index 1
set -g mouse on
set -g default-terminal "xterm-256color"
bind | split-window -h
bind - split-window -v
